/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/MAS-multi-agent-architectures',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;